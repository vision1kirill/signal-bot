[deploy]
startCommand = "python manage.py migrate --noinput && python manage.py collectstatic --noinput && python manage.py ensure_superuser && gunicorn signal_bot.wsgi:application --bind 0.0.0.0:$PORT --workers 1 --timeout 120"
